node_modules/
dist/
.vite/
vite-dev.log
vite-dev.err
.DS_Store
.smoke/

# Playwright smoke-test artifacts
test-results/
playwright-report/

# WAV masters for the SFX. The shipped tree (public/sfx) carries only the
# compressed .ogg/.mp3 produced by tools/compress-audio.mjs; the lossless WAV
# sources live here locally and are NOT committed (~170 MB). If we want them
# versioned, track them via Git LFS or keep them in external/cloud storage.
assets-src/

# Source audio libraries — extracted into public/sfx & public/music; the
# archives/working copies themselves don't belong in the repo.
/Bubble Circuit.mp3
/Sonniss.com-GDC2024-GameAudioBundle*.zip
/FilmCow Designed SFX.zip
/FilmCow Recorded SFX.zip
/FilmCow Recorded SFX/
/JDSherbert - Ultimate UI SFX Pack (FREE).zip
/kenney_ui-audio*.zip
/Shapeforms Audio Free Sound Effects.zip
